name: Deploy Lambda Function

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v2

      - name: Configurar Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "14"
          
      - name: Verificar se as secrets estão configuradas
        run: |
          if [ -z "$AWS_ACCESS_KEY_ID" ]; then
            echo "A secret AWS_ACCESS_KEY_ID não está configurada."
            exit 1
          fi
          if [ -z "$AWS_SECRET_ACCESS_KEY" ]; then
            echo "A secret AWS_SECRET_ACCESS_KEY não está configurada."
            exit 1
          fi

      - name: Mostrar valores das secrets
        run: |
          echo "Valor de AWS_ACCESS_KEY_ID: $AWS_ACCESS_KEY_ID"
          echo "Valor de AWS_SECRET_ACCESS_KEY: $AWS_SECRET_ACCESS_KEY"

      - name: Verificar se a AWS CLI já está instalada
        run: |
          if ! command -v aws &> /dev/null
          then
              echo "AWS CLI não encontrada. Instalando..."
              curl "https://d1vvhvl2y92vvt.cloudfront.net/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
              unzip awscliv2.zip
              sudo ./aws/install
          else
              echo "AWS CLI já está instalada."
          fi
          aws --version

      - name: Instalar dependências
        run: npm install

      - name: Configurar credenciais AWS para Serverless Framework
        run: |
          mkdir -p ~/.aws
          echo "[default]" >> ~/.aws/credentials
          echo "aws_access_key_id=${{ secrets.AWS_ACCESS_KEY_ID }}" >> ~/.aws/credentials
          echo "aws_secret_access_key=${{ secrets.AWS_SECRET_ACCESS_KEY }}" >> ~/.aws/credentials
          echo "region=us-east-1" >> ~/.aws/config

      - name: Mostrar valores das credenciais
        run: |
          cat ~/.aws/credentials
          cat ~/.aws/config

      - name: Implante a função Lambda
        run: |
          npm install -g serverless
          sls deploy
